apiVersion: apps/v1
kind: Deployment
metadata:
  name: aggregator
  labels:
    app: aggregator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aggregator
  template:
    metadata:
      labels:
        app: aggregator
        component: aggregator
    spec:
      containers:
        - name: main
          image: {{ .Values.aggregator.image }}
          command: ["mina-aggregator"]
          env:
            - name: CLUSTER_BASE_URL
              value: http://1.k8.openmina.com:31355
            - name: PLAIN_NODE_COUNT
              value: {{ .Values.plainNodesConfig.count | quote }}
            - name: SEED_NODE_COUNT
              value: "1"
            - name: SNARKER_NODE_COUNT
              value: {{ .Values.snarkWorkersConfig.count | quote }}
            - name: TRANSACTION_GENERATOR_NODE_COUNT
              value: "0"
            - name: PRODUCER_NODE_COUNT
              value: "5"
          ports:
            - name: http-aggregator
              containerPort: 8000
              protocol: TCP


